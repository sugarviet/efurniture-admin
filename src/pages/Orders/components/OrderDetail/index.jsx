import Proptypes from "prop-types";

const OrderDetail = ({ id }) => {

  return (
    <div>
    
    </div>
  );
};

OrderDetail.propTypes = {
  id: Proptypes.string,
};

export default OrderDetail;
